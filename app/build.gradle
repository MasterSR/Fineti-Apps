apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'Fineti'
            keyPassword 'Chipita'
            storeFile file('G:/Supriya/Fineti/Fineti_store_key.jks')
            storePassword 'MasterSR'
        }
    }
    compileSdkVersion 21
    buildToolsVersion "21.1.2"
    defaultConfig {
        applicationId 'com.fineti.myfineti.fineti'
        minSdkVersion 15
        targetSdkVersion 18
        versionName '14.9.18.9.20'
        versionCode 2
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }
}
dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.android.support:appcompat-v7:21.0.3'
    compile project(':volley')
}
